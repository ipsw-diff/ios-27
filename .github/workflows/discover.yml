name: Discover iOS 27 firmware

on:
  pull_request:
    paths:
      - .github/workflows/discover.yml
      - manifests/**
      - track.json
  workflow_dispatch:

permissions: {}

concurrency:
  group: ios-27-discovery-${{ github.ref }}
  cancel-in-progress: false

jobs:
  discover:
    permissions:
      contents: read
    uses: ipsw-diff/catalog/.github/workflows/discover.yml@abf25ba90c5591deff0b831c82f66711d978b0d5
    with:
      policy-path: track.json
      manifests-dir: manifests
